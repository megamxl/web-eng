services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:8080"
    environment:
      - BACKEND_URL=http://backend:3000/more-bears-rust
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"